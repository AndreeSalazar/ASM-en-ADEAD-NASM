// Test 5: Función completa con arrays y strings
fn procesar(arr, s, n):
    arr.append(n)
    let nuevo_s = s + " procesado"
    return len(arr) + len(nuevo_s)

let arr = [1, 2, 3]
let s = "texto"
let resultado = procesar(arr, s, 4)
print resultado

