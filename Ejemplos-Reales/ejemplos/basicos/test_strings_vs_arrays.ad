; Test comparación: strings vs arrays (ambos tienen len())
let arr = [1, 2, 3, 4, 5]
let s = "hola"

let len_arr = len(arr)
let len_s = len(s)

print len_arr
print len_s

; Ambos deberían funcionar con len()
if len_arr > 0 {
    print "Array tiene elementos"
}

if len_s > 0 {
    print "String tiene caracteres"
}

