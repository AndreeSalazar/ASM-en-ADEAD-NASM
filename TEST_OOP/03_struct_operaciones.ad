# Test 03: Operaciones con Structs
# Usar campos de struct en operaciones

struct Punto {
    x
    y
}

struct Rectangulo {
    ancho
    alto
}

let p1 = Punto { x: 10, y: 20 }
let p2 = Punto { x: 5, y: 15 }

# Sumar coordenadas
let suma_x = p1.x + p2.x
let suma_y = p1.y + p2.y
print suma_x
print suma_y

# Calcular área de rectángulo
let rect = Rectangulo { ancho: 10, alto: 5 }
let area = rect.ancho * rect.alto
print area

# Comparar
if p1.x > p2.x {
    print 1
} else {
    print 0
}

# Salida esperada:
# 15
# 35
# 50
# 1

